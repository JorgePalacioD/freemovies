// function openModal(videoId) {
//   var modal = document.getElementById("myModal");
//   var video = document.getElementById("video");
//   video.src = "https://www.youtube.com/embed/" + videoId;
//   modal.style.display = "block";
// }

// function closeModal() {
//   var modal = document.getElementById("myModal");
//   var video = document.getElementById("video");
//   video.src = "";
//   modal.style.display = "none";
// }

//   // Función para detener la reproducción del video al cerrar el modal
//     // Función para cargar el video correspondiente al botón clicado
//     $('#videoModal').on('show.bs.modal', function (event) {
//       var button = $(event.relatedTarget); // Botón que activó el modal
//       var videoSrc = button.data('video-src'); // Obtener la URL del video desde el botón
//       var videoIframe = document.getElementById('videoIframe');
//       videoIframe.src = videoSrc; // Establecer la URL del video en el iframe
//   });

//   // Función para detener la reproducción del video al cerrar el modal
//   $('#videoModal').on('hidden.bs.modal', function () {
//       var videoIframe = document.getElementById('videoIframe');
//       var videoSrc = videoIframe.src;
//       // Detener la reproducción del video
//       videoIframe.src = videoSrc;
//   });

// // // Primer modal
// // $('#videoModal1').on('show.bs.modal', function (event) {
// //   var button = $(event.relatedTarget);
// //   var videoSrc = button.data('video-src');
// //   var modal = $(this);
// //   modal.find('#videoIframe1').attr('src', videoSrc);
// // });

// // // Segundo modal
// // $('#videoModal2').on('show.bs.modal', function (event) {
// //   var button = $(event.relatedTarget);
// //   var videoSrc = button.data('video-src');
// //   var modal = $(this);
// //   modal.find('#videoIframe2').attr('src', videoSrc);
// // });

// Primer modal
$('#videoModal1').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe1').attr('src', videoSrc);
});

// Segundo modal
$('#videoModal2').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe2').attr('src', videoSrc);
});

$('#videoModal3').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe3').attr('src', videoSrc);
});

$('#videoModal4').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe4').attr('src', videoSrc);
});

$('#videoModal5').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe5').attr('src', videoSrc);
});
$('#videoModal6').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe6').attr('src', videoSrc);
});
$('#videoModal7').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe7').attr('src', videoSrc);
});
$('#videoModal8').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe8').attr('src', videoSrc);
});
$('#videoModal9').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe9').attr('src', videoSrc);
});
$('#videoModal10').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe10').attr('src', videoSrc);
});
$('#videoModal11').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe11').attr('src', videoSrc);
});
$('#videoModal12').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe12').attr('src', videoSrc);
});
$('#videoModal13').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe13').attr('src', videoSrc);
});
$('#videoModal14').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe14').attr('src', videoSrc);
});
$('#videoModal15').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe15').attr('src', videoSrc);
});
$('#videoModal16').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe16').attr('src', videoSrc);
});
$('#videoModal17').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe17').attr('src', videoSrc);
});
$('#videoModal18').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe18').attr('src', videoSrc);
});
$('#videoModal19').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe19').attr('src', videoSrc);
});
$('#videoModal20').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe20').attr('src', videoSrc);
});
$('#videoModal21').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe21').attr('src', videoSrc);
});
$('#videoModal22').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe22').attr('src', videoSrc);
});
$('#videoModal23').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe23').attr('src', videoSrc);
});
$('#videoModal24').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe24').attr('src', videoSrc);
});
$('#videoModal25').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe25').attr('src', videoSrc);
});

$('#videoModal26').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe26').attr('src', videoSrc);
});

$('#videoModal27').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe27').attr('src', videoSrc);
});

$('#videoModal28').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe28').attr('src', videoSrc);
});

$('#videoModal29').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe29').attr('src', videoSrc);
});

$('#videoModal30').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe30').attr('src', videoSrc);
});

$('#videoModal31').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe31').attr('src', videoSrc);
});

$('#videoModal32').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe32').attr('src', videoSrc);
});

$('#videoModal33').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe33').attr('src', videoSrc);
});

$('#videoModal34').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe34').attr('src', videoSrc);
});

$('#videoModal35').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe35').attr('src', videoSrc);
});

$('#videoModal40').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe40').attr('src', videoSrc);
});
$('#videoModal39').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe39').attr('src', videoSrc);
});
$('#videoModal38').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe38').attr('src', videoSrc);
});
$('#videoModal37').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe37').attr('src', videoSrc);
});
$('#videoModal36').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var videoSrc = button.data('video-src');
  var modal = $(this);
  modal.find('#videoIframe36').attr('src', videoSrc);
});

